# Project registry — user-specific story titles + personal file paths.
# Each user registers their own novels; not redistributed. See projects.py.
projects.json

# Personal workflow override (translator profile, house rules) — see WORKFLOW.md.
WORKFLOW.local.md

.venv/
__pycache__/
*.pyc

# Private requirements log from the original translation project — cited
# throughout this repo for rationale, but never shipped in it (the README and
# lint.py both already describe it as private). Contains the author's cast,
# per-character register rules and plot beats.
TRANSLATION-LESSONS.md
