# Upper bound is load-bearing: mcp 2.0.0 REMOVED `mcp.server.fastmcp`, which
# this server imports. An unbounded pin resolves to 2.0 on a fresh install and
# dies at import with ModuleNotFoundError.
mcp>=1.2.0,<2
requests>=2.31.0
# Only needed for the character_path mode (mask prep). Core tool works without.
pillow>=10.0.0
numpy>=1.24.0
# Needed by the helper tools (tools/make_sketch.py edge maps, fix_green, inpaint_region).
opencv-python-headless>=4.8.0
# tools/parallax.py: H.264/yuv420p encoding so clips play in browsers (bundles ffmpeg).
imageio-ffmpeg>=0.4.9
