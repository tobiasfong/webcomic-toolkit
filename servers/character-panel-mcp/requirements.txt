# Upper bound is load-bearing: mcp 2.0.0 REMOVED `mcp.server.fastmcp`, which
# this server imports. An unbounded pin resolves to 2.0 on a fresh install and
# dies at import with ModuleNotFoundError.
mcp>=1.2.0,<2
requests>=2.31.0
# Character Bible storage, palette extraction, and compose_panel's alpha compositing.
pillow>=10.0.0
# Tier-1 auto-matte (clean-backdrop pose renders -> RGBA cutouts). Downloads its own
# u2net ONNX model to the user's home dir on first use — no ComfyUI custom node needed.
# `rembg` alone does NOT pull in a working inference backend (silently fails at
# runtime with "No onnxruntime backend found") — onnxruntime must be explicit.
rembg>=2.0.50
onnxruntime>=1.16.0
# vrm_depth.py: reads the raw depth/normal EXR passes rendered by Blender
# (external dependency, NOT pip-installable — see vrm_depth.py's docstring for
# the separate Blender install this needs). numpy comes in transitively via
# rembg/onnxruntime already, but is used directly here too.
OpenEXR>=3.3.0
numpy>=2.0.0
