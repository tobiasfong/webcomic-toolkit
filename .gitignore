__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.env
.DS_Store
Thumbs.db

# generated debug artifacts from mannequin.render_pose_map / vrm_depth defaults
pose_map_*.png
vrm_depth_*.png

# private backups of the author's character bibles — never publish (repo is public)
_private_backups/

# visual novel game trees and story state — scripts carry character and world
# names, which must never reach the public repo
vn/
